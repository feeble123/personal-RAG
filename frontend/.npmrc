# 曾因项目路径含 & 字符（E:\GPT&Codex\...）导致 npm 脚本无法解析 node_modules/.bin，
# 改用 Git Bash 执行 npm 脚本。项目已迁移到 E:\GPT-Codex\LangChainRAG（无 &），
# 此补丁已无必要；且本机 Git Bash 缺 sed/dirname 等命令，反而导致 npm run dev 崩溃。
# 故 2026-09-05 起废弃（注释掉），npm 回到 Windows 默认方式跑脚本。
# script-shell=D:\Program Files\Git\usr\bin\bash.exe
