# 单元 O：评测尺子校准（gold 集标注修正）

> 制定日期：2026-08-31
> 前置：用户拍板「O 起步 + S 在 Q 前」，最终顺序 O→P→R→S→Q→T
> 目的：把「打分尺」修准——每道题的答案必须**真实存在于对应库**、锚点**指向正确章节**，
>       后续所有优化（召回/解析/存储）才能用评测数字证明「变好了」。

---

## 一、已核实的事实（连库查证，非猜）

### 1. 「滑坡勘察」题 —— 已经处理过，不用再做

- 单元 C 诊断时记「滑坡勘察标错库」，但**单元 L（502b8fb）已经删掉**了这题。
- 提交记录原文：`删「滑坡勘察」题(gold标错库，GB38509是设计规范无勘察章节)`。
- 当前 `gold_data.py` 里已无「勘察」二字 → ✅ 此条关闭。

### 2. 「建设目标」题 —— 答案不在库，待处理

`gold_data.py:107`「数字孪生农村供水工程的建设目标是什么？」，连库查证：

- 指南（库3）章节结构：`1 总则 / 1.1 范围`、`1 总则 / 1.2 规范性引用文件`，**没有「建设目标」章节**。
- 全文含「目标」的句子都是「以...为目标」（模型优化目标），**不是**「工程建设目标」。
- 结论：这题问的内容，库里**没有答案**（和「滑坡勘察」同病）。

处理方案（二选一，见文末待拍板）：
- **A 删掉**（同滑坡勘察，最干净）
- **B 换成「本指南的适用范围是什么？」**（`1.1 范围`，答案真实存在：「本指南主要适用于数字孪生农村规模化供水工程的规划、设计、建设、运行」）——能保住库3 的 general 意图覆盖（库3 现 5 题里 4 题是枚举，缺 general）

### 3. 「预警信息」题 —— note 标错章节 + 漏标锚点

`gold_data.py:244`「预警信息分为哪几个等级？」，note 写「7 附则 预警分级」，**标错了**。连库查证：

- 正确答案块是 **`3 预防预警 / 3.4 预警分级`**（p10）：「预警共划分为…六类。预警级别由高到低分为：特别重大（Ⅰ级）…」——这才是「分几级」的答案。
- `7 附则` 里的是「（一）Ⅰ级（红色）预警」这类**具体启动条件**，不是「分几级」。
- 当前 `expect_clauses=None`（漏标，应为 `"3.4"`），`expect_keywords=["预警","红色","橙色"]` 里「红色/橙色」也指向了错的 7 附则。

处理：note 改 `3 预防预警 / 3.4 预警分级`；`expect_clauses` 补 `["3.4"]`；`expect_keywords` 改成 3.4 原文实词（如 `["预警分级","六类","特别重大"]`）。

> 注：这题**同时**是单元 P 的「预警信息召回增强」对象。本单元只修**标注**（让尺子指对 3.4），召回策略（3.4 块进候选池）留单元 P。

---

## 二、锚点系统核查（逐题连库核对）

原则：`expect_clauses` 只在「答案明确落在某个可核对条款号」时才填；docx/xlsx 无条款号、general 题答案分散的，保持 None 是**合理**的，不强塞。

**静态扫描出的「可疑漏标/标错」清单**（需连库逐个核实后再改）：

| 题 | 库 | note 写的章节 | 现状 clauses | 疑点 |
|---|---|---|---|---|
| 什么是数字孪生 | 库2 | 3 术语和定义 | None | 可补 `["3"]` |
| 技术保障有哪些内容 | 库5 | 技术保障章节 | None | 实为 `5.11`，可补 |
| 预警信息分几级 | 库5 | 7 附则（错） | None | 实为 `3.4`，改 note + 补 |
| 滑坡稳定性验算要求 | 库7 | 规范正文 | None | 可能可补（对照 vague 同题锚点 `7`） |
| 管道埋深要求 | 库11 | 规范正文 | None | 可能可补（核实具体章号） |

其余 None 为合理（docx 库8 / xlsx 库9、库10 无条款号；库4 公式/条文说明等无单一章号）。

---

## 三、改动清单

1. `gold_data.py`：
   - 建设目标题：按待拍板结果「删」或「换成适用范围问法」。
   - 预警信息题：note + expect_clauses + expect_keywords 三处修正。
   - 锚点核查后，给「可疑漏标」清单里确实该补的题补 `expect_clauses`。
2. 重跑 `run_eval`，得到校准后的基线数字（记入 report.json + 进度）。

## 四、验收标准（数字说话）

- gold 集每题答案**真实存在于对应库**（连库抽查 100% 能定位到答案块）。
- 无「note 指向章节」与「expect_clauses」矛盾的题。
- 重跑 `run_eval` 得到可复现基线（严格条款 Recall@5 数字），作为后续单元 P/R/S/Q 的对比基准。
- pytest 全量绿（标注改动不影响代码路径）。

## 五、待拍板（不替您做主）

1. **建设目标题**：删掉（A）还是换成「适用范围」问法（B）？
2. **锚点核查范围**：只修上面列出的 5 个「可疑」题（小范围），还是把 60 问全部逐题连库核一遍（更彻底、更耗时）？

## 六、红线

- 只存 `feature/rag-optimization`，main 不动，不 push。
- 只读连库核对，不写生产数据。
- 完成后停等您确认，再进下一个单元。

---

## 七、执行结果（2026-08-31 收官）

### 7.1 实际修正（9 处）

| 题 | 原标注 | 修正为 | 类型 |
|---|---|---|---|
| 建设目标 | 答案不在库（无「建设目标」章节） | 换成「适用范围是什么？」→ `1.1` | 答案不在库 |
| 预警信息分几级 | note「7 附则」、clauses=None | note「3.4 预警分级」、clauses=`["3.4"]`、keywords 改 3.4 实词 | note 标错 + 漏锚点 |
| 明渠均匀流形成条件 | note「3.5 允许流速」 | note「3.1 形成条件」、clauses `3.5`→`3.1` | 张冠李戴 |
| 水循环 | keywords「水循环」 | 「水文循环」（原文实词） | 关键词标错 |
| 什么是数字孪生 | clauses=None | `["3"]` | 漏锚点 |
| 网络安全体系 | clauses `9.3`（太窄） | `["9"]` | 锚点过窄 |
| 应急响应分几级 | clauses `4.3` | `["4.1"]`（答案在 4.1 响应分级） | 张冠李戴 |
| 技术保障 | clauses=None | `["5.11"]` | 漏锚点 |
| 滑坡稳定性验算 | clauses=None | `["7"]` | 漏锚点 |

### 7.2 校准后基线（run_eval 单轮，真实 rerank）

| 指标 | 数值 | 说明 |
|---|---|---|
| Recall@5 | **98.3%** | 持平（1 题四方井失败，已记档 P3） |
| Citation | **100%** | — |
| 严格条款@5 | **82.2%**（45 题分母） | 比历史 84.6% 低 = 分母从 ~34 扩到 45（补了锚点），尺子变严暴露真实短板 |

### 7.3 校准暴露的真实短板（喂给后续单元）

8 题严格条款未过，分三类：

| 类别 | 题 | 归口 |
|---|---|---|
| 真实召回短板（答案在库，正确章节没进 top5） | 数据底板 6.1×2、应急响应分级 4.1、技术保障 5.11、预警分级 3.4、引用标准 3.2、流束理论 3 | → **单元 P（召回完整性）** |
| 四方井电气设备 Recall 失败 | hit=None | → 已记档 P3（用户放弃，不碰） |
| 前引部分 section 乱码（OCR） | 「2xxxxXxxx」 | → **单元 Q（解析完整性）** |

### 7.4 验收结论

- ✅ 59 题诊断全绿（0 可疑信号），每题的 clauses/keywords/note 均指向库内真实章节
- ✅ 无「note 指向章节」与「expect_clauses」矛盾
- ✅ pytest 569 passed + 1 skipped（标注改动不影响代码路径）
- ✅ 新增 2 个诊断工具 `gold_audit.py`（存在性）/ `gold_audit2.py`（章节反查），可复用

**单元 O 完成**，等用户确认后进单元 P（召回完整性）。
